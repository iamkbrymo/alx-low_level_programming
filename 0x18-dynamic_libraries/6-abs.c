#include "main.h"

/**
 * _abs - computes the absolute value of integers
 * @h: The integer to be computed
 * Return: The absolute value
 */
int _abs(int h)
{
	if (h >= 0)
		return (h);
	else
		return (-h);
}
