#include "main.h"

/**
 * add - Adds two integers and returns the result.
 * @k: the first number
 * @b: the second number
 * Return: the addition of both
 */

int add(int k, int b)
{
	return (k + b);
}
